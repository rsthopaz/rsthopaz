name: generate monkeytype readme svg

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  download-svg:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Download SVG
        run: |
          mkdir -p public

          curl -L -o public/monkeytype-readme.svg \
          https://monkeytype-production.up.railway.app/generate-svg/Tezt/lil_dragon

          curl -L -o public/monkeytype-readme-lb.svg \
          "https://monkeytype-production.up.railway.app/generate-svg/Tezt/lil_dragon?lb=true"

          curl -L -o public/monkeytype-readme-pb.svg \
          "https://monkeytype-production.up.railway.app/generate-svg/Tezt/lil_dragon?pb=true"

          curl -L -o public/monkeytype-readme-lb-pb.svg \
          "https://monkeytype-production.up.railway.app/generate-svg/Tezt/lil_dragon?lbpb=true"

      - name: Push SVGs
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: monkeytype-readme
          build_dir: public
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
